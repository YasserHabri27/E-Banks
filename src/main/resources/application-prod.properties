# Production Profile Configuration
# Activate with: -Dspring.profiles.active=prod

# Email Configuration (Real SMTP)
app.email.mode=REAL
spring.mail.password=vireyzlkumpbpfrc

# Logging (Cleaner for prod)
logging.level.org.springframework.security=WARN
logging.level.com.ebank=INFO
